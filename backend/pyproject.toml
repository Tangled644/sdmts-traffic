[project]
name = "sdmts-traffic"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "polars",

]

[project.optional-dependencies]
shape-vis = [
    "folium>=0.12",
    "geopandas",
    "mapclassify",
    "matplotlib",
]
data-vis = [
    "matplotlib",
]


[project.scripts]
sdmts-traffic = "sdmts_traffic:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
